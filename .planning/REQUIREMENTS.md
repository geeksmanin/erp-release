# Requirements: ERP Release Landing Page

## Scoped Requirements

- Fetch all releases from GitHub API (`/releases` instead of `/releases/latest`).
- Display a version list/selector or grid containing older versions.
- Label/mark the latest release as "stable" or "latest stable".
- Make all files/assets under each version downloadable.
